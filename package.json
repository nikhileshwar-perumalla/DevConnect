{
  "name": "devconnect-workspace",
  "version": "1.0.0",
  "description": "DevConnect - A social media platform for developers",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-deps": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
